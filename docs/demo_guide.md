## Guía del Demo

1. Preparar microSD y sistema.
2. Instalar dependencias con `scripts/install_dependencies.sh`.
3. Compilar (`scripts/build.sh`) y copiar cascade a `assets/haarcascades`.
4. Ejecutar `scripts/run_demo.sh`.
5. Analizar `logs/event_log.csv` y observar ventana HDMI.
6. Checklist de cierre y limpieza del entorno.
